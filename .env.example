# Database
# Pour développement avec SQLite:
DATABASE_URL="file:./dev.db"
# Pour production avec PostgreSQL:
# DATABASE_URL="postgresql://user:Arthur*0902pos@localhost:5432/decido?schema=public"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-a-secure-secret"

# ============================================
# EMAIL CONFIGURATION
# ============================================

# Choisissez un provider parmi: console, gmail, smtp, ethereal
# - console: Affiche les emails dans la console (par défaut, aucune config requise)
# - ethereal: Service de test gratuit, emails visibles en ligne (aucune config requise)
# - gmail: Utilise votre compte Gmail (nécessite EMAIL_USER et EMAIL_PASSWORD)
# - smtp: Serveur SMTP personnalisé (nécessite SMTP_HOST, SMTP_USER, SMTP_PASSWORD)

EMAIL_PROVIDER="console"

# Informations de l'expéditeur (pour tous les modes sauf console)
FROM_EMAIL="noreply@decido.app"
FROM_NAME="Decido"

# ============================================
# Option 1: Gmail (le plus simple avec un compte existant)
# ============================================
# EMAIL_PROVIDER="gmail"
# EMAIL_USER="votre.email@gmail.com"
# EMAIL_PASSWORD="votre-app-password"
#
# Note: Pour obtenir un App Password Gmail:
# 1. Allez sur https://myaccount.google.com/security
# 2. Activez la validation en 2 étapes
# 3. Générez un "App Password" pour "Mail"
# 4. Utilisez ce mot de passe de 16 caractères

# ============================================
# Option 2: SMTP personnalisé
# ============================================
# EMAIL_PROVIDER="smtp"
# SMTP_HOST="smtp.example.com"
# SMTP_PORT="587"
# SMTP_SECURE="false"
# SMTP_USER="your-smtp-username"
# SMTP_PASSWORD="your-smtp-password"

# ============================================
# Option 3: Ethereal (Test uniquement - AUCUNE CONFIG REQUISE)
# ============================================
# EMAIL_PROVIDER="ethereal"
# Les emails ne sont pas vraiment envoyés mais peuvent être visualisés en ligne
# L'URL de preview s'affiche dans la console au démarrage

# ============================================
# Option 4: Console (Développement - AUCUNE CONFIG REQUISE - PAR DÉFAUT)
# ============================================
# EMAIL_PROVIDER="console"
# Les emails sont juste affichés dans la console
# Parfait pour le développement local sans configuration
